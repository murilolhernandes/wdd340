<!-- Content heading -->
<% if (title) { %>
<h1><%= title %></h1>
<% } else {
  res.redirect('/')
} %>

<!-- Message block -->
<%- messages() %>

<!-- Form -->
<div class="form">
  <form action="/account/login" method="post">
    <fieldset>
      <legend>Login</legend>
      <div class="information">
        <label for="account_email">Email:</label>
        <input type="email" id="account_email" name="account_email" title="Your email address" autocomplete="email" required>
        
        <label for="account_password">Password:</label>
        <input type="password" id="account_password" name="account_password" title="Your password" autocomplete="current-password" required>
        <span id="show-password">Show Password</span>
        
        <input type="submit" value="Login">
        <p class="register">No account? 
        <a href="/account/register">Sign-up</a>
        </p>
      </div>
    </fieldset>
  </form>
</div>
